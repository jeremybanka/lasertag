import Exhibit from "c/Exhibit.astro";

# getting started

This route is a stub for installation and first-component guidance.

## install

Install `lasertag` alongside the JSX framework and lint tooling used by the
project.

## pair structure with styles

The exhibit pipeline is already live and reads examples directly from
`src/exhibits`.

<Exhibit src="getting-started/AppHeaderBar.tsx" />

<Exhibit src="getting-started/AppHeaderBar.module.css" />
