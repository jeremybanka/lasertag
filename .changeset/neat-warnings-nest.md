---
"lasertag": minor
---

Group `lasertag check` warnings by CSS Module, show source-attached explanations
with one line of surrounding context for up to ten affected files, and finish
stylish output with a color-aware accounting summary. Add `--max-files` to
choose another detail limit or show every affected file.
