---
"lasertag": minor
---

Group `lasertag check` warnings by CSS Module, show source-attached explanations
with one line of surrounding context for up to ten affected files, and finish
stylish output with a color-aware accounting summary. Add `--max-files` to
choose another detail limit or show every affected file. Add `--show-story` for
an opt-in view of the closest structural render possibilities, including a red
`✕ you are here` branch where each dead selector expected to land.
