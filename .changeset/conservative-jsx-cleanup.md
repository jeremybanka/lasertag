---
"lasertag": patch
---

Preserve potentially live CSS during JSX cleanup by respecting shadowed component and portal bindings, treating arbitrary callback factories as unknown render output, and accounting for explicit children and spread render props in transparent framework components.
