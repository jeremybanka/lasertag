---
"lasertag": minor
---

Report `selector-crosses-ownership-boundary` when CSS Module selectors can style children, render props, slots, or imported component DOM; analyze universal selectors structurally; and keep recognized portals outside the component's descendant story. This can surface warnings for selectors that earlier releases treated as inconclusive.
